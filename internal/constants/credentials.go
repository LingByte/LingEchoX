package constants

// CredentialPermissionWildcard means all catalog permissions when stored in permission_codes JSON.
const CredentialPermissionWildcard = "*"

// Credential lifecycle.
const (
	CredentialStatusActive   = "active"
	CredentialStatusDisabled = "disabled"
)
